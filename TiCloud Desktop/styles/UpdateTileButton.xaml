<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:svg="http://sharpvectors.codeplex.com/svgc/">




    <Style x:Key="FocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle Margin="0,0,0,0" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1" Width="72" Height="380" />
                </ControlTemplate>
            </Setter.Value> 
        </Setter>
    </Style>
    <SolidColorBrush x:Key="Button.Static.Background" Color="#FFFEFEFE"/> <!-- Kolor tła -->
    <SolidColorBrush x:Key="Button.Static.Border" Color="#FFFEFEFE"/> <!-- Kolor obramowania -->

    <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FFFEFEFE"/> <!-- Kolor tła (po najechaniu mysza)-->
    <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FFFEFEFE"/> <!-- Kolor obramowania (po najechaniu mysza)-->

    <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFFEFEFE"/> <!-- Kolor tła (po naciśnięciu)-->
    <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FFFEFEFE"/> <!-- Kolor obramowania (po naciśnięciu)-->

    <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFFEFEFE"/> <!-- Kolor tła (zablokowanego) -->
    <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFFEFEFE"/> <!-- Kolor obramowania (zablokowanego) -->

    <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FFF2F2F2"/> <!-- Kolor tekstu -->
    <Style x:Key="UpdateTileButton.xaml" TargetType="{x:Type Button}">
        <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
        <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        <Setter Property="BorderThickness" Value="0"/> <!-- Grubość obramowania -->
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true" CornerRadius="15,15,15,15" Width="380" Height="72" Margin="0,0,0,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Margin="10" CornerRadius="7" Width="52" Height="52" Background="#FF0D0D0D">
                                <svg:SvgViewbox Source="{Binding IconPath}" Width="38" Height="38"/>
                            </Border>
                            <!-- Tekst -->
                            <!-- <TextBlock Text="tekst 1" FontWeight="Bold" FontSize="16"/> -->
                            <!-- <TextBlock Text="tekst 2" FontSize="12"/> -->
                            <!-- <TextBlock Text="tekst 3" FontSize="10" Foreground="Gray"/> -->
                            
                            
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </StackPanel>
                        
                    </Border>
                
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsDefaulted" Value="true">
                            <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" TargetName="border" Value="{StaticResource Button.MouseOver.Background}"/>
                            <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background}"/>
                            <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                            <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                            <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>